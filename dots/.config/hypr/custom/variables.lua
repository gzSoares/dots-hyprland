-- Override default variables here. Loaded BEFORE custom/* and hyprland/*
-- defaults so you can flip the dontLoadDefault* gates or rebind the app
-- globals (terminal, fileManager, browser, etc.) defined in
-- hyprland/variables.lua.

-- Disable any of the default modules (see hyprland.lua):
-- dontLoadDefaultExecs    = true
-- dontLoadDefaultGeneral  = true
-- dontLoadDefaultRules    = true
-- dontLoadDefaultColors   = true
-- dontLoadDefaultKeybinds = true

-- Override an app launcher chain — e.g. force Alacritty as terminal:
-- terminal = "kitty"
-- browser  = "brave"
